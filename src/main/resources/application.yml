spring:
  application:
    name: rest-spring
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost/rest_spring?useTimezone=true&serverTimezone=UTC
    username: root
    password: flac2525
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        dialect:
      show-sql: false

      flyway:
        enabled: true
        locations: classpath:db/migration

logging:
  level:
    #root: WARN
    rest_springboot.rest_spring: DEBUG


